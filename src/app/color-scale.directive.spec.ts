import { ColorScaleDirective } from './color-scale.directive';

describe('ColorScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorScaleDirective();
    expect(directive).toBeTruthy();
  });
});
